简易的命令行入门教程:
Git 全局设置:

git config --global user.name "backpropagation"
git config --global user.email "icompiler@qq.com"
创建 git 仓库:

mkdir fhe-transformer
cd fhe-transformer
git init 
touch README.md
git add README.md
git commit -m "first commit"
git remote add origin https://gitee.com/backpropagation/fhe-transformer.git
git push -u origin "master"
已有仓库?

cd existing_git_repo
git remote add origin https://gitee.com/backpropagation/fhe-transformer.git
git push -u origin "master"
